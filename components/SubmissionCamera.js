import React, { Component } from 'react';
import { Dimensions, Text, View, Vibration, TouchableOpacity, TouchableHighlight } from 'react-native';
import { Flex, Icon, WingBlank, WhiteSpace } from 'antd-mobile';
import styles from '../styles';

class SubmissionCamera extends Component {
  render() {
    return (
      <View style={styles.container}>
        <Text>working...</Text>
      </View>
    );
  }
}

export default SubmissionCamera;
